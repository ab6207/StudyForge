# Leave empty for local dev (Vite proxies /api to http://localhost:5050 - see vite.config.js)
# Set to your deployed backend's URL when frontend and backend are hosted separately, e.g.:
# VITE_API_URL=https://study-forge-backend.vercel.app
VITE_API_URL=
